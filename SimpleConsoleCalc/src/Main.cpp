#include <iostream> // Library for outputting data

int main()
{
	// Testing and looging hello world to console
	std::cout << "hello world" << std::endl;

	// Wait for user to press anything so he can see message
	std::cin.get();
	// Exit with status 0 if everything fine
	return 0;
	//lol
}